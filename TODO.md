# TODO

## Step 1 ✅
- Fix runtime bug in `Frontend/digitaltwin/src/components/NewTest.js` where `index` is referenced but not defined.

## Step 2 ✅
- Hardening: make dropdown rendering resilient to missing soil fields (id/name/soil_type).


## Step 3
- Test:
  - Start backend (`cd Backend && npm start`)
  - Start frontend (`cd Frontend/digitaltwin && npm start`)
  - Navigate to New Test view and verify dropdown renders without errors.

